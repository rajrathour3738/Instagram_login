{
  "name": "instagram-login",
  "version": "1.0.0",
  "description": "An Instagram-like login page with Node.js and Express.js",
  "main": "app.js",
  "scripts": {
    "start": "node app.js"
  },
  "dependencies": {
    "express": "^4.17.1", // Assuming you're using Express for your backend
    "body-parser": "^1.19.0", // Middleware to parse incoming request bodies
    "sqlite3": "^5.0.2" // SQLite3 for database interaction
    // Add any other dependencies you're using
  },
  "devDependencies": {},
  "engines": {
    "node": "14.x" // Specify the Node.js version (optional)
  },
  "private": true
}
